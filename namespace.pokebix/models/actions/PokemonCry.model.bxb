action (PokemonCry) {
  description (Plays the pokemon's cry)
  type (Search)
  collect {
    input (Pokemon_name) {
      type (Pokemon_name)
      min (Required) max (One)
    }
  }
  output (audioPlayer.AudioInfo)
}
