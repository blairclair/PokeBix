action (andWhatColor) {
  type (Search)
  description (What color is the pokemon?)
  collect {
    input (Pokemon) {
      type (Pokemon)
      min (Required) max (One)
    }
  }
  output (Pokemon_color)
}
