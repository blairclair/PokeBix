action (findPokemonSpecies) {
  type (Fetch)
  description (finds the pokemon species)
  collect {
    input (Pokemon) {
      type (Pokemon)
      min (Required) max (One)
    }
  }
  output (Pokemon)
}
