action (findPokemonByName) {
  description (Finds a pokemon by name)
  type (Search)
  collect {
    input (Pokemon_name) {
      type (Pokemon_name)
      min (Required) max (One)
    }
  }
  output (Pokemon)
}
